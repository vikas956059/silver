on: push
jobs:
  ssh_deploy:
    name: ssh deploy
    runs-on: ubuntu
    setps:
     name: file compile
     uses: action/checkout@v3

     name: sycn files
     uses: easingthemes/ssh-deploy@v2.1.4
     with: 
       SSH_PRIVATE_KEY: ${{ secrets.SECRETS }}
       REMOTE_HOST: 3.109.184.32
       REMOTE_HOST: root
       SOURCE: "./"
       TARGET: /var/www/html
